{
  "name": "agentique-browser-extension",
  "version": "1.0.0",
  "description": "Agentique - Role-based context injection for AI chats",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r popup.html app.js background.js manifest.json components screens services content-scripts design-system icons assets dist/",
    "lint:css": "stylelint \"**/*.css\" --ignore-path .stylelintignore",
    "lint:css:fix": "stylelint \"**/*.css\" --fix --ignore-path .stylelintignore"
  },
  "devDependencies": {
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0"
  },
  "keywords": [
    "browser-extension",
    "chrome-extension",
    "ai",
    "roles"
  ]
}
